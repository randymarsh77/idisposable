// swift-tools-version:5.1
import PackageDescription

let package = Package(
    name: "IDisposable",
    products: [
        .library(
            name: "IDisposable",
            targets: ["IDisposable"]),
    ],
    targets: [
        .target(
            name: "IDisposable",
            dependencies: []),
    ]
)
